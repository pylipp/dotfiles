#!/usr/bin/env bash

sdd_install() {
    cd /tmp || return 1
    git clone https://github.com/pylipp/dzr
    cp -v dzr/dzr dzr/dzr-url dzr/dzr-dec "$SDD_INSTALL_PREFIX"/bin
    rm -rfv dzr
}

sdd_uninstall() {
    rm -fv "$SDD_INSTALL_PREFIX"/bin/drz
    rm -fv "$SDD_INSTALL_PREFIX"/bin/drz-dec
    rm -fv "$SDD_INSTALL_PREFIX"/bin/drz-url
}

sdd_fetch_latest_version() {
    _sha_of_github_master pylipp drz
}

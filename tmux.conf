##################
# GLOBAL OPTIONS #
##################

set-option -g default-shell /usr/bin/zsh
set-option -g default-terminal "screen-256color"
set-option -gs escape-time 0

# https://gist.github.com/MohamedAlaa/2961058
setw -g mode-keys vi

# if-shell syntax acc. to https://stackoverflow.com/a/40030702/3865876 and
# https://shapeshed.com/custom-vim-bindings-in-tmux-2-4/
### in copy-mode (started with prefix [):
# v to begin visual selection
# y to copy visual selection to clipboard
# C-v to toggle rectangular selection
# paste to xterm using Ctrl-Shift-v
if-shell "tmux -V | awk '{exit !($2 >= \"2.4\")}'" \
    ' \
    bind-key -Tcopy-mode-vi v send -X begin-selection; \
    bind-key -Tcopy-mode-vi y send -X copy-selection-and-cancel'

# < v2.5; putting this as if-shell-else block did not work
if-shell "tmux -V | awk '{exit !($2 < \"2.4\")}'" \
    ' \
    bind-key -t vi-copy v begin-selection; \
    bind-key -t vi-copy C-v rectangle-toggle; \
    bind-key -t vi-copy y copy-selection'

# large history
set-option -g history-limit 10000

# border colors
# setw -g mode-bg black
set -g status-bg colour244
set -g pane-border-fg colour244
set -g pane-active-border-fg green
set -g pane-active-border-bg default

# show current window written in white on orange bg
set -g status-justify left
setw -g window-status-current-fg colour9
setw -g window-status-current-bg white
setw -g window-status-current-attr reverse
setw -g window-status-current-format '#I|#W#F'
setw -g window-status-format '#I|#W#F'
 
set -g status-left-length 16
# not setting these yet bc I'm ok with the default
# set-option -g status-left '» #T#[default]'
# set-option -g status-right '#[fg=red,bold][[ #(git branch) branch ]] #[fg=cyan]»» #[fg=blue,bold]###S #[fg=magenta]%R %m-%d#(acpi | cut -d ',' -f 2)#[default]'
set -g status-right '#($TMUX_PLUGIN_MANAGER_PATH/tmux-mem-cpu-load/tmux-mem-cpu-load.py)#[default]'
set -g status-right-length 60

# Automatically set window title
setw -g automatic-rename

# Information in terminal title: hostname, session name, current window name,
# current pane index, current pane title
set-option -g set-titles on
set-option -g set-titles-string '#H:#S.#W.#P #T'


############
# BINDINGS #
############

# http://unix.stackexchange.com/questions/130730/how-to-quickly-send-commands-to-nested-tmux-sessions
bind-key -n C-S-b send-prefix

# reload config
bind-key r source-file ~/.tmux.conf

bind c new-window -c '#{pane_current_path}'
bind '"' split-window -v -c '#{pane_current_path}'
bind % split-window -h -c '#{pane_current_path}'

# toggle between last active panes (alternatively go to last active window)
bind-key P last-pane
#bind-key P if-shell 'test #{window_panes} -gt 1' 'last-pane' 'last-window'
# list sessions: prefix s
# list windows:  prefix w

# toggle between last active windows: prefix l
# toggle between last active sessions: prefix L
 
# break pane: prefix :break-pane or prefix !
# %% = prompt for window.pane [-V|H] # vert|hor split
bind-key J command-prompt "joinp -t:%%" 

###########
# PLUGINS #
###########

# install:   prefix I
# update:    prefix U
# uninstall: prefix alt-u
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-copycat'
set -g @plugin 'tmux-plugins/tmux-yank'
set -g @plugin 'pylipp/tmux-mem-cpu-load'

# Smart pane switching with awareness of Vim splits.
set -g @plugin 'christoomey/vim-tmux-navigator'

set -g @copycat_hash_search 'C-h'
set -g @copycat_ip_search 'C-i'

set -g @yank_selection 'clipboard'
set -g @shell_mode 'vi'

set -g @resurrect-strategy-vim 'session'

# Initialize tmux plugin manager (keep line at the very bottom)
run '~/.tmux/plugins/tpm/tpm'
